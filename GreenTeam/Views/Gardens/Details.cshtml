@model GreenTeam.ViewModels.GardenVM

@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.Name)</></h1>
<h4>@Html.DisplayFor(model => model.Location)</></h4>


<p>
    <a asp-controller="Patches" asp-action="Create" asp-route-id="@Model?.Id">Add Patch</a> |
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit Garden</a> |
    <a asp-action="Index">Back to List</a>
</p>

<table class="table">

     <thead>
        <tr>
            <th>Patch Name:</th>
            <th>Crop Name:</th>  
        </tr>
    </thead>
   @foreach(var item in Model.Patches)
    {
    <tbody>
        <tr>
             <td><a asp-controller="Patches" asp-action="Edit" asp-route-id="@item.Id">
                @Html.DisplayFor(modelItem => item.PatchName)
                </a>
            </td>
            <td>
               <a asp-controller="Patches" asp-action="Edit" asp-route-id="@item.Id">
                @Html.DisplayFor(modelItem => item.Crop)
                </a>
            </td>
       </tr>
    </tbody>
    }
    <thead class="MemberTable">
        <tr>
            <th>Members:</th>
        </tr>
    </thead>
      @*@foreach(var item in Model.Users)
    {
    <tbody>
    
        <tr>
             <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
             <td>
            </td>
       </tr>
           
    </tbody>
    }*@
    </table>
