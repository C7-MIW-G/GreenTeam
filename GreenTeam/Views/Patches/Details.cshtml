@model GreenTeam.ViewModels.PatchVM
@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.PatchName)</></h1> 

<p>
    <a class="btn btn-action" asp-controller="PatchTasks" asp-action="Create" asp-route-id="@Model?.Id">Add Task</a>
    <a class="btn btn-action"asp-controller="Gardens" asp-action="Details" asp-route-id="@Model?.Id">Back to List</a>
</p>

<table class="table">

     <thead>
        <tr>
            <th>Task:</th>
            <th>Description:</th>  
        </tr>
    </thead>
   @foreach(var item in Model.PatchTasks)
    {
    <tbody>
        <tr>
             <td><a asp-controller="PatchTasks" asp-action="Edit" asp-route-id="@item.Id">
                @Html.DisplayFor(modelItem => item.TaskName)
                </a>
            </td>
            <td>
               <a asp-controller="PatchTasks" asp-action="Edit" asp-route-id="@item.Id">
                @Html.DisplayFor(modelItem => item.TaskDescription)
                </a>
            </td>
       </tr>
    </tbody>
    }
</table>
